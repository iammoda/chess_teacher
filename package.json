{
  "scripts": {
    "secret-scan": "node scripts/secret-scan.mjs",
    "start": "node server.js",
    "smoke": "node scripts/browser-smoke.mjs",
    "test": "npm run secret-scan && node --test tests/*.test.js tests/*.test.mjs"
  },
  "devDependencies": {
    "playwright": "^1.60.0"
  }
}
